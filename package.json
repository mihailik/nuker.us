{
  "dependencies": {
    "esbuild": "^0.24.2",
    "three": "^0.172.0",
    "troika-three-text": "^0.52.3"
  },
  "scripts": {
    "build-rust": "wasm-pack build ./rust --target web --out-dir ../wasm",
    "build-js": "esbuild"
  }
}
